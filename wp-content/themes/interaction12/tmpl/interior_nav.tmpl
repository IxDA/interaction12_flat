<nav class="interior-nav nav ${section}" id="${section}-nav"><div class="wrap">
  <ul class="menu" id="menu-${section}-menu">
  {{each pages}}
    {{if iframe}}
    <li><a href="${href}">${title}</a></li>
    {{else}}
    <li><a href="#${id}" data-c-actions="scrollTo">${title}</a></li>
    {{/if}}
  {{/each}}
  </ul>
  <a href="#${section}-nav" data-c-actions="toggleClass toggleSetting" data-class="pinned" data-setting="pinned" class="toggle" title="Click to pin this menu open">toggle</a>
</div></nav>